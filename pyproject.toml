[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio>=4.0.0",
    "openai-whisper>=20231117",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]
